apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ .Namespace }}-baseline-network-policy
  namespace: {{ .Namespace }}
  annotations:
    platformctl.io/todo: "Replace this MVP policy with stricter app-specific ingress and egress rules."
spec:
  podSelector: {}
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - from:
        - podSelector: {}
  egress:
    - {}
